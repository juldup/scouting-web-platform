import { Routes } from '@angular/router';
import { AppComponent } from './app.component';

export const routes: Routes = [
  { path: 'gestion/presences-angular', component: AppComponent },
  // other routes...
];