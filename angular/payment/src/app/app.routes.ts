import { Routes } from '@angular/router';
import { AppComponent } from './app.component';

export const routes: Routes = [
  { path: 'gestion/paiement-angular', component: AppComponent },
  // other routes...
];